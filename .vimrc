" source junas
source ~/.vim/junas

" install whatever bundles over the net
silent call Bundle(["powerline"])
silent call Bundle(["surround", "repeat", "matchit.zip", "eregex"])

" use the xoria256 color scheme (included)
colorscheme xoria256
